### Key Idea

Check the currently logged-in account:

### What Matters

- --resource-group → Resource group name
- --name → VM name
- --image → OS image (Ubuntu 22.04)
- --size → VM size
- --admin-username → SSH login username

### Quick Summary

If not logged in: Set your resource group as a variable:
