### Key Idea

I use Resend as a mail provider in Laravel to send emails using an API-based approach instead of traditional SMTP.

### What Matters

- faster
- more reliable
- easier to scale
- Laravel prepares the email
- Sends it via Resend API

### Quick Summary

Instead of SMTP: 👉 Laravel sends emails through Resend API
