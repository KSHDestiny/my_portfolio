### Key Idea

I use Task Scheduling to automate recurring jobs without manually managing cron tasks for each command.

### What Matters

- define scheduled tasks in application
- system cron runs every minute
- scheduler checks which tasks should run
- executes the due tasks
- sending daily/weekly reports

### Quick Summary

Instead of writing multiple cron jobs: 👉 I define all scheduled tasks in one place
