# Notion integration token (optional — the site falls back to local data without it)
NOTION_TOKEN=

# Project cards — powers lib/projects.ts when set
NOTION_PROJECTS_DATABASE_ID=

# Knowledge notes — required by `npm run sync:knowledge`
NOTION_KNOWLEDGE_DATABASE_ID=
